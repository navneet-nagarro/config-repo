spring:
  datasource:
    url: jdbc:mysql://mysql:3306/basic_customer
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

server:
  port: 8081

eureka:
  client:
    enabled: false
    server-url:
      defaultZone: http://eureka-server:8761/eureka/
